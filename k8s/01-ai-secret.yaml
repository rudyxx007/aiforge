# Kubernetes Secret for the Gemini API Key
apiVersion: v1
kind: Secret
metadata:
  name: gemini-api-key-secret
type: Opaque
data:
  # Placeholder for the pipeline's 'sed' command to replace
  GEMINI_API_KEY: {{secrets.GEMINI_API_KEY}}
