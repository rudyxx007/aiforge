# Kubernetes Secret to hold the Gemini API key (populated by pipeline)
apiVersion: v1
kind: Secret
metadata:
  name: gemini-api-key-secret
type: Opaque
data:
  # Pipeline will inject the Base64 encoded API key here
  GEMINI_API_KEY: {{secrets.GEMINI_API_KEY}}